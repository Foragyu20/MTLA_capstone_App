name: mtla_2
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  audioplayers: ^5.2.0
  file_picker: ^6.0.0
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.0.0
  flutter_tts: ^3.8.3
  http: ^1.1.0
  provider: ^6.0.5
  shared_preferences: ^2.2.1
  speech_to_text: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/
    - assets/audio/
    - assets/audio2/
    - assets/quiz_questions.txt